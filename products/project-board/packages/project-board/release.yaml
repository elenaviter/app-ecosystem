package:
  id: project-board
  ref: "2026.09.22.2241"
  description: |
    First working client release. The distribution installs `pb`, owns the
    complete host client and worker procedure, and supplies the shared
    contract imported by the server. A per-target selector binds both the
    command and supervised relay to either this exact package version or one
    verified App Ecosystem commit containing every imported package.

components:
  python_package:
    id: project-board
    path: products/project-board/packages/project-board
    version: "2026.09.22.2241"
