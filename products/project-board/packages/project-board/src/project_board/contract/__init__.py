"""Shared Project Board protocol and identity contracts.

The server and the ``pb`` client import these modules from one distribution so
reference grammar, worker identity, operation outcomes, and wire shapes cannot
drift between the two sides.
"""
