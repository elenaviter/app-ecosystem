package:
  id: service-foundation
  ref: "2026.09.22.2029"
  description: |
    First published release of the implementation candidate: the generic
    service boundary used with app-foundation by Connection Hub and the
    Problem Board client. Published so those clients can be installed from
    an index instead of copied from a checkout.

components:
  python_package:
    id: service-foundation
    path: packages/service-foundation
    version: "2026.09.22.2029"
